to run Frontend (NextJs) : npm run dev 

to run Backend (FastAPI/Python): python -m uvicorn main:app --reload --port 8000


### Quick Start
1. Install MongoDB locally or use MongoDB Atlas
2. Configure connection string in `backend/.env`
3. Run backend with MongoDB enabled
4. All user data, conversations, and messages are now saved to the database!
